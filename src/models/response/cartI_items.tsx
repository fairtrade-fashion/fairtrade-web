export interface CartItem {
  id: string;
  name: string;
  image: string;
  selectedSize: string;
  selectedColor: string;
  amount: number;
  quantity: number;
  description: string;
  content: string;
}
